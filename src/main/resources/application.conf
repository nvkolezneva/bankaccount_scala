database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10

    properties = {
      serverName = "localhost"
      portNumber = 5432
      databaseName = "scala-bankaccount"
      user = "postgresuser"
      password = "mysecretpass"
    }
  }
}